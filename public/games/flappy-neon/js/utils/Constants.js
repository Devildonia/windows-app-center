export const CONFIG = {
    V_HEIGHT: 1000,
    GRAVITY: 2000,        // Suavizado (era 2500)
    JUMP_FORCE: -750,     // Suavizado (era -900)
    WORLD_SPEED: 450,     // Base — escala con score
    PIPE_GAP: 320,        // Base — reduce con score
    PIPE_SPACING: 550,
    PIPE_WIDTH: 140,
    // Dificultad progresiva
    SPEED_MAX: 650,       // Velocidad máxima
    GAP_MIN: 230,         // Gap mínimo
    DIFF_PIPES: 25        // Pipes para llegar al máximo
};
